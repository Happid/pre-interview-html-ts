### A. Scope, menentukan di bagian mana variabel bisa diakses.
1. Global Scope => Variabel yang dibuat di luar fungsi & block — bisa diakses dari mana saja.
2. Function Scope => Variabel yang didefinisikan DI DALAM function hanya bisa diakses di dalam function itu saja.
3. Block Scope (let & const) => Variabel dalam { } hanya bisa diakses di dalam block itu.

### B. Lexical Environment, Sebuah function akan SELALU ingat di scope mana dia dibuat (dasar dari closure).

### C. Call Stack
